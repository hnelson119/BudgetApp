# Local container administration uses the PostgreSQL Unix socket. Every TCP
# connection must negotiate TLS and authenticate with SCRAM; plaintext TCP has
# an explicit rejection rule and no permissive fallback.
local all all trust
hostssl all all all scram-sha-256
hostnossl all all all reject
