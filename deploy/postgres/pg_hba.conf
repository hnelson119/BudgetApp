# Local container administration uses the PostgreSQL Unix socket. Every TCP
# connection must negotiate TLS and present a client certificate issued by the
# dedicated client CA. The exact certificate identity-to-role map is generated
# by the startup wrapper. Plaintext TCP has an explicit rejection rule.
local all all trust
hostssl all all all cert map=budget_service
hostnossl all all all reject
